name: JavaScript Tests

on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
    
    - name: Создание package.json
      run: |
        cat > package.json << 'EOF'
        {
          "name": "js-test",
          "version": "1.0.0",
          "scripts": {
            "test": "node test.js"
          }
        }
        EOF
    
    - name: Создание тестов
      run: |
        # Основной тестовый файл
        cat > test.js << 'EOF'
        const assert = require('assert');
        
        // Тестируемые функции
        const math = {
          add: (a, b) => a + b,
          subtract: (a, b) => a - b,
         
